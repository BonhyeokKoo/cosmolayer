name: docs
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base dependencies
  - python
  - pip
  - numpy
  - pyyaml

    # Machine learning dependencies
  - pytorch
  - lightning
  - opt-einsum

    # Testing
  - pytest
  - pytest-cov

  # Docs building
  - sphinx
  - sphinxcontrib-bibtex
  - sphinx-copybutton
