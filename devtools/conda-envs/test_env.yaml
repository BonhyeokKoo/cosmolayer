name: test
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pyyaml
  - typing-extensions

    # Testing
  - pytest
  - pytest-cov

    # Machine learning dependencies
  - pytorch
  - lightning
  - opt-einsum
